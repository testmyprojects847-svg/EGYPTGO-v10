'use client'

import { useEffect, useState } from 'react'
import { Pencil, Plus, ShieldCheck, Star, Trash2, UserRound, X } from 'lucide-react'
import { useLanguage } from '@/hooks/useLanguage'
import { guideService } from '@/services/guides/guideService'
import type { Guide } from '@/types'

const input = 'rounded-xl border border-input bg-background px-3 py-3 text-sm outline-none focus:ring-2 focus:ring-ring'
const blank: Partial<Guide> & { name: string } = { name: '', nameAr: '', city: 'Cairo', language: 'English, Arabic', specialty: 'Egyptology', specialtyAr: 'علم المصريات', rating: '5.0', reviews: 0, price: 50, image: '/images/guides/layla.png', verified: true, bio: '', bioAr: '', availability: true, assignedTours: 0 }

export function GuideWorkspace() {
  const { locale, isRtl } = useLanguage(); const [guides, setGuides] = useState<Guide[]>([]); const [editing, setEditing] = useState<(Partial<Guide> & { name: string }) | null>(null)
  useEffect(() => setGuides(guideService.list()), [])
  const set = (key: keyof Guide, value: unknown) => setEditing((current) => current ? { ...current, [key]: value } : current)
  const save = (event: React.FormEvent<HTMLFormElement>) => { event.preventDefault(); if (!editing?.name.trim()) return; setGuides(guideService.save(editing, editing.id)); setEditing(null) }
  const fields: [keyof Guide, string, boolean][] = [['name', 'Name', true], ['nameAr', 'Arabic name', false], ['city', 'City / location', true], ['language', 'Languages', false], ['specialty', 'Specialty', true], ['specialtyAr', 'Arabic specialty', false], ['price', 'Hourly rate', true], ['image', 'Photo URL', true], ['bio', 'Bio', false], ['bioAr', 'Arabic bio', false]]
  return <section className="flex flex-col gap-6"><div className="flex flex-col gap-4 sm:flex-row sm:items-center sm:justify-between"><div><p className="text-xs font-semibold uppercase tracking-[0.18em] text-accent">People & expertise</p><h2 className="mt-1 text-2xl font-bold">Guides directory</h2><p className="mt-1 text-sm text-muted-foreground">Create, edit, verify, and manage local experts.</p></div><button onClick={() => setEditing({ ...blank })} className="inline-flex items-center justify-center gap-2 rounded-xl bg-primary px-4 py-3 text-sm font-semibold text-primary-foreground"><Plus className="size-4" /> Add guide</button></div><div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{guides.map((guide) => <article key={guide.id} className="overflow-hidden rounded-2xl border border-border bg-card shadow-sm"><div className="flex gap-4 p-5"><img src={guide.image} alt={guide.name} className="size-20 rounded-2xl object-cover" /><div className="min-w-0 flex-1"><div className="flex items-start justify-between"><div><h3 className="font-bold">{locale === 'ar' ? guide.nameAr : guide.name}</h3><p className="text-xs text-muted-foreground">{guide.city} · {locale === 'ar' ? guide.specialtyAr ?? guide.specialty : guide.specialty}</p></div>{guide.verified && <ShieldCheck className="size-4 text-accent" />}</div><div className="mt-3 flex items-center gap-3 text-xs"><span className="inline-flex items-center gap-1 font-semibold"><Star className="size-3 fill-accent text-accent" /> {guide.rating} ({guide.reviews})</span><span className="text-muted-foreground">${guide.price}/hr</span></div></div></div><div className="flex justify-end gap-1 border-t border-border px-5 py-3"><button aria-label={`Edit ${guide.name}`} onClick={() => setEditing({ ...guide })} className="rounded-lg p-2 text-muted-foreground hover:bg-muted"><Pencil className="size-4" /></button><button aria-label={`Delete ${guide.name}`} onClick={() => window.confirm(`Delete ${guide.name}?`) && setGuides(guideService.remove(guide.id))} className="rounded-lg p-2 text-muted-foreground hover:bg-destructive/10 hover:text-destructive"><Trash2 className="size-4" /></button></div></article>)}</div>{!guides.length && <div className="rounded-2xl border border-dashed border-border p-12 text-center"><UserRound className="mx-auto text-muted-foreground" /><p className="mt-3 font-semibold">No guides yet</p></div>}{editing && <div className="fixed inset-0 z-50 flex items-end justify-center bg-primary/30 sm:items-center sm:p-6"><form onSubmit={save} dir={isRtl ? 'rtl' : 'ltr'} className="max-h-[92vh] w-full max-w-2xl overflow-y-auto rounded-t-3xl bg-card p-6 shadow-2xl sm:rounded-3xl"><div className="flex items-center justify-between"><div><h3 className="text-xl font-bold">{editing.id ? 'Edit guide' : 'Add guide'}</h3><p className="text-sm text-muted-foreground">Profile, expertise, and verification.</p></div><button type="button" onClick={() => setEditing(null)} aria-label="Close"><X /></button></div><div className="mt-6 grid gap-4 sm:grid-cols-2">{fields.map(([key, label, required]) => <label key={key} className="flex flex-col gap-2 text-xs font-semibold">{label}<input required={required} type={key === 'price' ? 'number' : 'text'} value={String(editing[key] ?? '')} onChange={(event) => set(key, key === 'price' ? Number(event.target.value) : event.target.value)} className={input} /></label>)}<label className="flex items-center gap-3 text-sm font-semibold"><input type="checkbox" checked={editing.verified ?? true} onChange={(event) => set('verified', event.target.checked)} /> Verified guide</label><label className="flex items-center gap-3 text-sm font-semibold"><input type="checkbox" checked={editing.availability ?? true} onChange={(event) => set('availability', event.target.checked)} /> Available for bookings</label></div><div className="mt-6 flex justify-end gap-3"><button type="button" onClick={() => setEditing(null)} className="rounded-xl border border-border px-4 py-3 text-sm font-semibold">Cancel</button><button className="rounded-xl bg-primary px-4 py-3 text-sm font-semibold text-primary-foreground">Save guide</button></div></form></div>}</section>
}
